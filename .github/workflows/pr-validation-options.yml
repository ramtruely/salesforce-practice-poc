heavy-checks:
  runs-on: ubuntu-latest
  needs: detect
  if: needs.detect.outputs.validation_level == 'full'

  steps:
    - name: Package Coverage
      run: echo "coverage"

    - name: Risk Assessment
      run: echo "risk"

    - name: Unit Tests
      run: echo "tests"

      ----
      package-coverage:
  runs-on: ubuntu-latest
  needs: detect
  if: needs.detect.outputs.validation_level == 'full'
  steps:
    - run: echo "coverage"

risk-assessment:
  runs-on: ubuntu-latest
  needs: detect
  if: needs.detect.outputs.validation_level == 'full'
  steps:
    - run: echo "risk"

unit-tests:
  runs-on: ubuntu-latest
  needs: detect
  if: needs.detect.outputs.validation_level == 'full'
  steps:
    - run: echo "tests"

    ---

    unit-tests:
  runs-on: ubuntu-latest

package-coverage:
  runs-on: ubuntu-latest
  needs: unit-tests

risk-assessment:
  runs-on: ubuntu-latest
  needs: package-coverage
